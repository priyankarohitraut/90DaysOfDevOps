Create users and set passwords
1. sudo useradd tokyo
   2. sudo useradd berlin
      3. sudo useradd professor
         
1. sudo passwd tokyo
2. sudo passwd berlin
3. sudo passwd professor
   

Create groups and assign users
sudo groupadd developer
sudo groupadd admins
 assign user 
 sudo uermod -ag developer tokyo
 sudo uermod -ag admins berlin
 
 
Set up shared directories with group permissions



Use what you learned from Days 1-7 to find the right commands!

